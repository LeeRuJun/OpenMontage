# Compose Director

Render 16:9 1080p for YouTube using the approved runtime. Prefer Remotion for motion graphics and captions; use FFmpeg only
when Remotion is unavailable. Verify duration, audio loudness, caption timing, playable output, and a silent-view structure check.

